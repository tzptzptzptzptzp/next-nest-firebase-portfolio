import { nameType } from "@/types/data.type"

export const UserName = ({ data }: { data: nameType }) => {
  return (
    <>
      <div className="text-center">
        <h2 className="text-4xl font-bold">テツカ ヒロキ</h2>
        <p>tzp</p>
      </div>
    </>
  )
}